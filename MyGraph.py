import matplotlib.pyplot as plt #missing libraries, we need to install it, separate from the root
import numpy as np

x = np.linspace(0,20,100)
plt.plot(x, np.sin(x))

plt.show()

print("HELLO WORLD.")